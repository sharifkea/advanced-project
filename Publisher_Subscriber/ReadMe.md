# Packages required
Install packages: pip install pyzmq pandas numpy xgboost scikit-learn.
# Port
To see the task on port5556: netstat -ano | findstr :5556
To kill the task on port5556: taskkill /PID 28624 /F 
# Run Subscriber first


